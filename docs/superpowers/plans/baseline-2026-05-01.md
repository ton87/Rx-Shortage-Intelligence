# Restructure Baseline — 2026-05-01

Captured at branch `restructure/2026-05-01` HEAD = `e42f2f4e` (worktree at `../rx-restructure`).

## Test status

```
269 pass / 7 fail / 1 collection error
```

### Failures (pre-existing, environmental)

All 7 `test_run_agent_*` in `tests/test_h3_h4_briefing.py::TestRunAgent` fail because the test env has no `ANTHROPIC_API_KEY`. Tests instantiate `anthropic.AsyncAnthropic()` without mocking, hitting `Could not resolve authentication method`.

- `test_run_agent_appends_to_tool_call_log`
- `test_run_agent_calls_tool_on_tool_use`
- `test_run_agent_handles_tool_exception_gracefully`
- `test_run_agent_non_tool_stop_reason_breaks_loop`
- `test_run_agent_respects_max_iterations`
- `test_run_agent_result_preview_truncated`
- `test_run_agent_returns_text_on_end_turn`

These are NOT caused by restructure. Restructure must keep them at the same status (no new failures).

### Collection error

`tests/test_h5_ui_helpers.py` cannot import — references nonexistent `load_briefing` and `SORT_ORDER` from `src.main`. Will be fixed in Task 7.1.

## Prompt cache key baselines (R2 mitigation)

```
ROLE_AND_RULES:  68f482f157934367850cafef4bde40e4c58fc2fbd725505a14af2d358750df9a
SEVERITY_RUBRIC: 802ae28a8aa0d062c6b32d52b85dd0ac8733de475d96fbbd6f886ea73024b00c
```

After Task 4.2 + 4.3 (prompt extraction to markdown), `load_prompt("role_and_rules")` and `load_prompt("severity_rubric")` must produce these exact SHA-256 hashes. Drift = prompt cache invalidation.

## Briefing JSON shape baseline

Snapshot at `/tmp/briefing-baseline.json` (988 lines). Top-level keys must be preserved exactly across Task 8.1 verification.
